1. 介绍，安装
2. 体系结构
	数据库：只有一个库
	实例：后台运行一个进程
	表空间：逻辑存储单位       C，D，E，F 逻辑分区
	数据文件：物理存储单位	硬盘
	用户：面向用户管理，由用户管理表空间，表空间对应数据文件 ，用户向表空间添加数据，最终存储到数据文件中
3. 表空间（创建），用户管理：创建，权限管理（connect ，resource，dba）
4. 表的管理（数据类型，创建表，修改表，sql语句分类，DML，序列（sequence:一张表对应一个序列））
5. scott: 介绍三张表
6. 单行函数: 一行记录返回一个结果
	
7. 多行函数（分组函数，聚合函数）: 多行记录返回一个结果
8. 内连接，外连接(特殊的外连接：)
9. 子查询：返回一个结果，返回一列多行，返回多行多列
10. 分页查询： rownum，伪列，生成行号，从1开始
	
	
	